{
  "description": "Championship-level training configuration based on DeepStack paper specifications",
  "num_buckets": 169,
  "bucket_count": 169,
  "hidden_sizes": [500, 500, 500, 500, 500, 500, 500],
  "activation": "prelu",
  "data_path": "src/train_samples_championship",
  "batch_size": 1024,
  "effective_batch_size": 4096,
  "use_gpu": true,
  "lr": 0.0005,
  "min_lr": 1e-6,
  "epochs": 200,
  "weight_decay": 0.01,
  "warmup_epochs": 10,
  "huber_delta": 0.3,
  "ema_decay": 0.999,
  "early_stop_patience": 25,
  "early_stop_min_delta": 1e-6,
  "use_street_weighting": true,
  "street_weights": [0.6, 1.0, 1.4, 2.0],
  "use_torch_compile": true,
  "deterministic": false,
  "seed": 42,
  "versions_dir": "models/versions",
  "checkpoint_dir": "models/checkpoints",
  "comments": {
    "description": "Championship-level configuration optimized for Texas Hold'em",
    "hidden_sizes": "7 layers × 500 units per DeepStack paper Table S2",
    "batch_size": "Physical batch 1024, accumulated to 4096 for stable gradients",
    "lr": "Conservative with warmup and cosine decay",
    "huber_delta": "Lower delta (0.3) for tighter fit in standardized space",
    "street_weights": "Aggressive weighting [0.6, 1.0, 1.4, 2.0] - emphasize later streets",
    "ema": "Exponential moving average for better generalization",
    "early_stop_patience": "25 epochs patience for full convergence",
    "expected_results": {
      "correlation": ">0.85",
      "relative_error": "<5%",
      "calibration_slope": "0.9-1.1",
      "sign_mismatch": "<10%"
    },
    "data_requirements": "500K+ samples with CFR iterations >= 2500"
  }
}
