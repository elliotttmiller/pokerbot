# Poker AI Championship Pipeline - Colab Dependencies
# ===================================================
# Optimized for Google Colab Free Tier with T4 GPU
# Install with: pip install -r requirements.txt

# Core Dependencies (Colab-compatible versions)
numpy>=1.26.4,<2.0.0
pydantic>=2.0.0
pyyaml>=6.0

# PyTorch (Colab pre-installed, but specify for compatibility)
torch>=2.0.0
torchvision>=0.15.0

# Hugging Face Transformers
transformers>=4.36.0
accelerate>=0.25.0

# Unsloth - Memory-efficient fine-tuning
# Note: Install separately in Colab with specific wheel
# pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"

# BitsAndBytes - 4-bit quantization
bitsandbytes>=0.41.0

# PEFT - Parameter-Efficient Fine-Tuning
peft>=0.7.0

# TRL - Training library for LLMs
trl>=0.7.0

# Vision Support
pillow>=10.0.0
qwen-vl-utils>=0.0.8

# Data Processing
datasets>=2.16.0
pandas>=2.2.2

# Progress and Logging
tqdm>=4.66.0
tensorboard>=2.15.0

# ONNX Export (Optional)
onnx>=1.15.0
onnxruntime>=1.16.0

# Testing
pytest>=7.4.0

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.0

# Configuration
python-dotenv>=1.0.0
